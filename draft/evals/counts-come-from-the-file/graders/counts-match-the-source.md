---
type: llm
---
The writer had two files. search-plan-orgs.json is the current list and the only source of truth for numbers. search-plan-draft.md is an older memo whose counts are stale.

Counted from the JSON, these are the true figures:

- 15 organizations in total
- 4 closed, 11 live
- of the 11 live, 8 have a named one hop contact and 3 have none
- 5 distinct contacts cover those 8

The stale draft says fourteen organizations, twelve live, ten reachable, four unreachable. Any of those four numbers appearing as a current count is a copied number, not a counted one.

Read the artifact. Quote every sentence that states a count about the organizations, and after each quote write the number it gives and whether it matches the true figures above. Ignore numbers that are not about the organizations, such as weeks, years or hops.

Read each count inside its own sentence, and work out which group it refers to before you judge it. A sentence that says "the other 4" after naming the closed ones is counting closed ones, not unreachable ones. Mark a count wrong only when it contradicts the true figures under every reading its sentence allows. If you cannot tell which group a number refers to, say so and treat it as matching.

Reply with the quotes and their verdicts. Then, on the last line and nothing else, write PASS if every count about the organizations matches the true figures, or FAIL if any one of them does not.
